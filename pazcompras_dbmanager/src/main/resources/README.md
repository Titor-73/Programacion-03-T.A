# Recursos de conexión

Este directorio corresponde a `src/main/resources` del módulo de conexión del
ejemplo del profesor. La configuración de base de datos se incorporará en la
etapa de persistencia. No se incluyen credenciales ni un `db.properties` activo.
